name: Generate contribution snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Generate snake SVGs
        uses: Platane/snk@v3
        with:
          github_user_name: sahilmathur254
          outputs: |
            dist/snake-dark.svg?color_snake=#00E5FF&color_dots=#0d1b24,#0e3d47,#0a6b7a,#0090a5,#00b3c8
            dist/snake-light.svg?color_snake=#0097a7&color_dots=#e8f7fa,#a9e2ec,#5cc6d6,#1fa2b5,#00788a

      - name: Push SVGs to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}